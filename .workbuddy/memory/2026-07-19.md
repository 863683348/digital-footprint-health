# 2026-07-19 工作日志

## 完成上线清单 #4 亮黑 UI、#8 移动端（构建+推送）、#9 SEO
- 用户指令："#4 #8 #9 先去完成"。
- #4 深色模式（CSS 变量换肤方案）：
  - `tailwind.config.ts` 加 `darkMode:'class'`，且把所有颜色改为引用 CSS 变量（primary/primary-weak/ok/warn/danger/ink/ink-soft/line/surface/canvas 全映射 `var(--color-*)`）。
  - `app/globals.css` 新增 `.dark { … }` 变量集（参考 GitHub Dark：bg #0B0F14 / surface #161B22 / line #2A323D / ink #E6EAF0 / primary #4C7BF0 等），并加 `.dark .bg-ok-weak/.bg-warn-weak/.bg-danger-weak` 深色微染底。
  - 新增 `components/ThemeProvider.tsx`（light/dark，localStorage 记忆 + 跟随系统 prefers-color-scheme）。
  - `app/layout.tsx` 包 `<ThemeProvider>`，注入 no-flash 内联脚本（首屏前定 dark class）。
  - `components/NavBar.tsx` 加明暗切换按钮，用 Lucide `Moon`/`Sun` 图标（设计系统禁 emoji，故不用 ☾/☀ 字符）。
  - 因颜色走 CSS 变量，组件几乎无需逐个加 `dark:` 类，全站自动换肤。
- #9 SEO：
  - 新增 `app/sitemap.ts`（MetadataRoute.Sitemap，首页/upload/删除确认）。
  - 新增 `app/robots.ts`（allow `/`，disallow /report/ 与 /delete/progress，含 sitemap 指向）。
  - `app/layout.tsx` 加 `metadataBase` + `openGraph` + `twitter` + `alternates.canonical:'/'`。
  - `app/page.tsx` 注入 JSON-LD（`WebApplication` schema，含 offers/inLanguage）。
- #8 移动端（收尾）：此前响应式 CSS 已写未验证；本次 `npm run build` 通过（11 路由，含 sitemap.xml/robots.txt），`npm run test` 6/6 通过，`tsc --noEmit` 通过。commit `6e8d092` + push `origin/main`。
- 部署坑：本地 `next build` 首次被 safe-delete 守卫拦截（清 .next 触发 ETIMEDOUT）；先 `rm -rf .next`（dangerouslyDisableSandbox）再 build 即过。
- commit `b9b69bf` 更新 `docs/launch-checklist.md` 状态。

## 阻塞：Vercel 线上重新部署（#8 最后一步）
- 推完代码后线上 URL `https://dfh-fgbk5c1y2-863683348s-projects.vercel.app` 仍返回 410 "The deployment has been removed"——旧部署被 Vercel 回收，且 GitHub push 未自动冒出新生产部署（或旧部署已删新未起）。
- 本环境无 Vercel 凭证：vercel CLI 已装但 `vercel --version` 可用、无登录态；无 `VERCEL_TOKEN`、无 `~/.vercel/auth`；`.vercel/project.json` 的 projectName 是 `2026-07-13-10-03-45`（与线上 `dfh-mvp/dfh-fgbk5c1y2` 不符，疑似早期 link 残留）。
- 待办：需用户再次提供 Vercel token（`vcp_…`）由我跑 `vercel deploy --prod`；或用户在 Vercel Dashboard 手动 Redeploy。代码侧 #8 已全部就绪。
- 重新部署后：建议手机访问做移动端真机回归；到 Google Search Console 提交 sitemap.xml（GSC 验证域名不在代码范围）。
