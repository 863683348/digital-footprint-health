// Single source of truth for the production site URL.
// Used by metadata, sitemap, robots, and JSON-LD so the domain never drifts.
export const SITE_URL = 'https://digital-footprint-health.vercel.app';
