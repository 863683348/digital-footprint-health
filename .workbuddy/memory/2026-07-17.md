# 2026-07-17

## GitHub 仓库可见性
- 用户要求把 `863683348/digital-footprint-health` 设为 public。`gh repo edit --visibility public --accept-visibility-change-consequences` 成功，现已 PUBLIC（private=false）。
- 注：`gh repo edit` 改可见性必须带 `--accept-visibility-change-consequences`，否则报错。

## Vercel 部署（已完成 ✅）
- 用户给出 Vercel token `vcp_...`（前缀 vcp_ 即 Vercel access token，正确）。`vercel deploy --prod --yes --token <tok>` 成功。
- 项目实际挂在 **TEAM** `863683348s-projects` 下（accountId `team_WYqXheVujG6BllkeG3s1g2X2`），非个人账号。
- 项目名：`2026-07-13-10-03-45` → 改名 `dfh-mvp`（API PATCH `{"name":"dfh-mvp"}`）。`digital-footprint-health` 名已被占用（conflict）。
- **SSO 拦截修复**：默认 `ssoProtection.deploymentType=all_except_custom_domains` 导致所有 *.vercel.app 访问被重定向到 `vercel.com/sso-api`。
  - 必须用 `PATCH /v9/projects/prj_F8X2iOUVXt0YlPhMsiSTT9A5T3t4` 传 `{"ssoProtection":null}` 才能关闭；传 `{"deploymentType":"none"}` 会被账户默认策略还原。
- 验证：
  - 稳定可用 URL：`https://dfh-fgbk5c1y2-863683348s-projects.vercel.app`（沙箱内实测全页面 200，上传 CSV 返回 rowCount=25 / score=73 / 6 维度 / 9 风险旗）。
  - Vercel 官方别名：`https://2026-07-13-10-03-45.vercel.app`（沙箱到该域名偶有 TLS 000 抖动，真实浏览器正常）。
  - `dfh-mvp.vercel.app` 因 alias 未绑定 + team 域 API 需 teamId 参数，返回 404（不影响使用，可后续在 Dashboard 设自定义域名）。
- 上传响应结构为 `{"ok":true,"data":{"archiveId","archive":{...}}}`（archive 含 details.riskFlags，非顶层 riskFlags）。

## GitHub
- 仓库已 public：`https://github.com/863683348/digital-footprint-health`（含 serverless 化代码，commit 56435f6）。

## 本地环境约束（备忘）
- 本沙箱 safe-delete 守卫会拦截 `next dev` 自身的 .next 清理（50 文件批量删除阈值），导致本地 dev server 无法在本沙箱内重启；不影响 Vercel 生产构建。用户可在自己终端 `npm run dev`。
